# Use default Node (cached nodejs_20) to avoid fetching large nixpkgs for nodejs_22 (saves disk during setup)
# Keep install --no-optional to reduce disk use during npm install
[phases.install]
cmds = ["npm install --no-optional"]
