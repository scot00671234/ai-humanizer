# Force Node 22 (avoids cached nodejs_20; pdfjs-dist requires >=20.19 or >=22)
[phases.setup]
nixPkgs = ["nodejs_22", "npm-9_x"]

# Fewer optional deps to reduce disk use; if ENOSPC persists, free disk or clear Docker build cache on the server
[phases.install]
cmds = ["npm install --no-optional"]
